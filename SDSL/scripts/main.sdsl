# implicit using global;
using MathLib;

# File scoped namespace for less nesting
namespace Test;

class Program {
    # Entry point 
    static func main() {
        for c: String in "hello" {
            Console.println(new Number(c));
        }
    }
}