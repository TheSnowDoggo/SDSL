using global;

namespace global {
    class Nil {}
    class Bool {}
    class Number {}
    class String {}
}

namespace Project {
    class Program {
        static const FILE_PATH: String = "/home/test";
        var x: Number = 0;
        var y: Number = 43;
        const str: String = "hello";
        
        new(const x, const y: Number) {
            self.x = x;
            self.y = y;
        }
        
        func get_thing(x: global::String) -> Thing {
        }
        
        func test(const money: global::Number) {
            var x: global::String = FILE_PATH; // implicit Project::Program.FILE_PATH
            var y = get_thing(x); // implicit self.get_thing(x)
            
            get_thing(self.x); // explicit self.x refers to member, not local variable x
        }
    }
}