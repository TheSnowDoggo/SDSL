using Test;

namespace Test {
    class Foo {
        static const AHH;
    }
    class Hello {
        static func foo() {
        }
    }
}

namespace Project {
    class Program {
        static const FILE_PATH = "/home/test";
        
        new(const x, const y: Number) {
            self.x = x;
            self.y = y;
        }
        
        static func get_object() {}
        
        static func test() {
            const field = FILE_PATH;
        }
        
        #*
        func get_thing(x: String) -> Thing {
        }
        
        func test2(const money: Number) {
            var x: String = FILE_PATH; # implicit Project::Program.FILE_PATH
            var y = get_thing(x); # implicit self.get_thing(x)
            
            get_thing(self.x); # explicit self.x refers to member, not local variable x
        }
        *#
    }
}