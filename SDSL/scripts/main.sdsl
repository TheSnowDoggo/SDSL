# implicit using global;
using MathLib;

# File scoped namespace for less nesting
namespace Test;

class Program {
    # Entry point 
    static func main() {
        # Console is in global namespace
        Console.println(new Vec2(2, 3).to_string());
    }
}